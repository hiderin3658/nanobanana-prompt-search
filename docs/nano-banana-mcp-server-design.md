# Nano Banana Pro プロンプト検索 MCPサーバー設計書

## 📋 ドキュメント情報

| 項目 | 内容 |
|------|------|
| 作成日 | 2026年1月6日 |
| バージョン | 1.0 |
| 対象 | Claude.ai / Claude Desktop / Claude Mobile |

---

## 1. プロジェクト概要

### 1.1 目的

Nano Banana Pro（Gemini画像生成）のプロンプト集をClaude上から意味検索できるリモートMCPサーバーを構築する。

### 1.2 ゴール

- Claude.aiのIntegrationsから利用可能なMCPサーバーをVercel上にデプロイ
- 無料枠内での運用（Vercel Hobby + Upstash Vector Free）
- 日本語・英語両方のプロンプトを意味検索可能に

### 1.3 前提条件

- Claude Pro/Max/Team/Enterprise プラン（リモートMCPサーバー利用に必須）
- Vercelアカウント（GitHub連携）
- Upstashアカウント（ベクトルDB用）

---

## 2. データソース選定

### 2.1 Upstash Vector 無料枠

| 項目 | 制限 |
|------|------|
| ベクトル数 | **10,000件/月** |
| クエリ数 | 10,000回/日 |
| 次元数 | 最大3,072 |

### 2.2 リポジトリ比較と選定

| リポジトリ | プロンプト数 | ライセンス | 特徴 | 選定 |
|-----------|-------------|-----------|------|------|
| **ZeroLu/awesome-nanobanana-pro** | 約50件 | MIT | 高品質・厳選、カテゴリ整理済 | ✅ 採用 |
| **xianyu110/awesome-nanobananapro-prompts** | 908件 | MIT | 大量収録、中国語中心 | ✅ 採用 |
| Ionic-Errrrs-Code/awesome-nanobanana-pro-prompts | 10,000+ | MIT | 大量だが無料枠超過の可能性 | ❌ 見送り |
| YouMind-OpenLab/awesome-nano-banana-pro-prompts | 中規模 | CC BY 4.0 | 商用利用可だが要クレジット表記 | △ 将来検討 |

### 2.3 ベクトル数見積もり

```
ZeroLu:    約   50件
xianyu110: 約  908件
─────────────────────
合計:      約  958件 → 無料枠 10,000件の約10%
```

**結論**: 2つのリポジトリを統合しても無料枠内で十分余裕あり。将来の拡張にも対応可能。

---

## 3. システムアーキテクチャ

### 3.1 全体構成図

```
┌─────────────────────────────────────────────────────────────┐
│                      ユーザー環境                            │
├─────────────────────────────────────────────────────────────┤
│  Claude.ai (Web)  │  Claude Desktop  │  Claude Mobile      │
│      Pro+         │     Pro+         │     Pro+            │
└────────┬──────────┴────────┬─────────┴────────┬────────────┘
         │                   │                   │
         │         MCP Protocol (HTTPS)         │
         │                   │                   │
         ▼                   ▼                   ▼
┌─────────────────────────────────────────────────────────────┐
│              Vercel (リモートMCPサーバー)                    │
│                      Hobby枠 - 無料                         │
├─────────────────────────────────────────────────────────────┤
│  /api/mcp          - MCPエンドポイント                      │
│  /api/sync         - データ同期API (Cron Job)               │
│  /api/health       - ヘルスチェック                         │
└────────────────────────────┬────────────────────────────────┘
                             │
              ┌──────────────┴──────────────┐
              │                             │
              ▼                             ▼
┌─────────────────────────┐   ┌─────────────────────────────┐
│    Upstash Vector       │   │        GitHub               │
│    (ベクトルDB)          │   │    (データソース)            │
│    Free枠 - 無料         │   │                             │
├─────────────────────────┤   ├─────────────────────────────┤
│ • 10,000ベクトル        │   │ • ZeroLu/awesome-           │
│ • 意味検索              │   │   nanobanana-pro            │
│ • メタデータ付き         │◀──│ • xianyu110/awesome-        │
│                         │   │   nanobananapro-prompts     │
└─────────────────────────┘   └─────────────────────────────┘
```

### 3.2 データフロー

```
[日次同期 - Vercel Cron]
GitHub README.md → パース → Embedding生成 → Upstash Vector

[検索リクエスト]
ユーザークエリ → Embedding変換 → Upstash Vector検索 → 結果返却
```

---

## 4. 技術スタック

### 4.1 インフラ・サービス

| レイヤー | 技術 | プラン | 月額コスト |
|---------|------|--------|-----------|
| ホスティング | Vercel | Hobby | $0 |
| ベクトルDB | Upstash Vector | Free | $0 |
| Embedding | Upstash Embedding | Free枠内 | $0 |
| データソース | GitHub API | 無料 | $0 |
| **合計** | | | **$0** |

### 4.2 開発技術

| 項目 | 技術 |
|------|------|
| 言語 | TypeScript |
| フレームワーク | Next.js (App Router) |
| MCPライブラリ | @modelcontextprotocol/sdk |
| ベクトルDB SDK | @upstash/vector |
| パッケージマネージャ | npm / pnpm |

### 4.3 Vercel Hobby枠の制限

| 項目 | 制限 |
|------|------|
| 帯域幅 | 100GB/月 |
| Serverless Function実行時間 | 10秒/リクエスト |
| Cron Jobs | 2回/日まで（Hobbyプラン） |
| ビルド時間 | 6000分/月 |

**注意**: プロンプト検索程度の用途であれば十分な制限。

---

## 5. MCPツール設計

### 5.1 ツール一覧

| ツール名 | 説明 | パラメータ |
|---------|------|-----------|
| `search_prompts` | プロンプトを意味検索 | `query`, `category?`, `limit?` |
| `list_categories` | カテゴリ一覧を取得 | なし |
| `get_prompt_detail` | プロンプト詳細を取得 | `id` |

### 5.2 ツール定義（TypeScript）

```typescript
// search_prompts
{
  name: "search_prompts",
  description: "Nano Banana Pro のプロンプトを意味検索します。日本語・英語どちらでも検索可能です。",
  inputSchema: {
    type: "object",
    properties: {
      query: {
        type: "string",
        description: "検索キーワードまたは説明文（例: '商品写真を撮りたい', 'anime portrait'）"
      },
      category: {
        type: "string",
        description: "カテゴリで絞り込み（オプション）",
        enum: ["photorealism", "creative", "education", "ecommerce", "marketing", "avatar", "other"]
      },
      limit: {
        type: "number",
        description: "取得件数（デフォルト: 5, 最大: 20）",
        default: 5
      }
    },
    required: ["query"]
  }
}

// list_categories
{
  name: "list_categories",
  description: "利用可能なプロンプトカテゴリの一覧を取得します",
  inputSchema: {
    type: "object",
    properties: {}
  }
}

// get_prompt_detail
{
  name: "get_prompt_detail",
  description: "特定のプロンプトの詳細情報を取得します",
  inputSchema: {
    type: "object",
    properties: {
      id: {
        type: "string",
        description: "プロンプトID"
      }
    },
    required: ["id"]
  }
}
```

### 5.3 レスポンス形式

```typescript
// search_prompts レスポンス例
{
  results: [
    {
      id: "zerolu-1-5",
      title: "One-Click Business Photo (Silicon Valley Style)",
      category: "photorealism",
      prompt: "Keep the facial features of the person...",
      source: "ZeroLu/awesome-nanobanana-pro",
      score: 0.92
    },
    // ...
  ],
  total: 5
}
```

---

## 6. データ構造

### 6.1 ベクトルDBスキーマ

```typescript
interface PromptVector {
  id: string;           // "zerolu-1-5" or "xianyu-15"
  vector: number[];     // 1536次元 (text-embedding-3-small)
  metadata: {
    title: string;      // プロンプトタイトル
    prompt: string;     // プロンプト本文
    category: string;   // カテゴリ
    source: string;     // リポジトリ名
    sourceUrl: string;  // 元URL
    language: string;   // "en" | "zh" | "ja"
    imageUrl?: string;  // サンプル画像URL（あれば）
  }
}
```

### 6.2 カテゴリマッピング

| カテゴリID | 表示名 | 説明 |
|-----------|--------|------|
| photorealism | 写真・肖像 | 写実的な写真、ポートレート |
| creative | クリエイティブ | アート、実験的な表現 |
| education | 教育・図解 | インフォグラフィック、教材 |
| ecommerce | EC・商品 | 商品撮影、バーチャル試着 |
| marketing | マーケティング | 広告、SNS用画像 |
| avatar | アバター・SNS | プロフィール画像、キャラクター |
| interior | インテリア | 部屋のデザイン、家具配置 |
| editing | 写真編集 | レタッチ、背景除去 |
| other | その他 | 上記に該当しないもの |

---

## 7. デプロイ手順

### 7.1 事前準備

#### Step 1: Upstash Vector インデックス作成

1. [Upstash Console](https://console.upstash.com/) にログイン
2. 「Vector」→「Create Index」
3. 設定:
   - Name: `nano-banana-prompts`
   - Region: `us-east-1`（または最寄り）
   - Dimensions: `1536`（text-embedding-3-small用）
   - Similarity: `COSINE`
4. 作成後、以下をメモ:
   - `UPSTASH_VECTOR_REST_URL`
   - `UPSTASH_VECTOR_REST_TOKEN`

#### Step 2: GitHubリポジトリ作成

```bash
# リポジトリ作成
gh repo create nano-banana-mcp --public --clone
cd nano-banana-mcp

# 初期化
npm init -y
npm install @modelcontextprotocol/sdk @upstash/vector next
npm install -D typescript @types/node
```

### 7.2 プロジェクト構成

```
nano-banana-mcp/
├── app/
│   └── api/
│       ├── mcp/
│       │   └── route.ts        # MCPエンドポイント
│       ├── sync/
│       │   └── route.ts        # データ同期API
│       └── health/
│           └── route.ts        # ヘルスチェック
├── lib/
│   ├── mcp-server.ts           # MCPサーバー実装
│   ├── vector-store.ts         # Upstash Vector操作
│   ├── github-parser.ts        # GitHubデータパーサー
│   └── types.ts                # 型定義
├── scripts/
│   └── seed-data.ts            # 初期データ投入スクリプト
├── vercel.json                 # Vercel設定
├── package.json
├── tsconfig.json
└── README.md
```

### 7.3 主要ファイル

#### vercel.json

```json
{
  "crons": [
    {
      "path": "/api/sync",
      "schedule": "0 0 * * *"
    }
  ]
}
```

#### 環境変数（Vercel Dashboard で設定）

```
UPSTASH_VECTOR_REST_URL=https://xxx.upstash.io
UPSTASH_VECTOR_REST_TOKEN=xxx
```

### 7.4 Vercelへのデプロイ

#### Step 1: GitHubにプッシュ

```bash
git add .
git commit -m "Initial commit: Nano Banana MCP Server"
git push origin main
```

#### Step 2: Vercelでインポート

1. [Vercel Dashboard](https://vercel.com/dashboard) にログイン
2. 「Add New...」→「Project」
3. GitHubリポジトリを選択
4. 環境変数を設定:
   - `UPSTASH_VECTOR_REST_URL`
   - `UPSTASH_VECTOR_REST_TOKEN`
5. 「Deploy」をクリック

#### Step 3: 初期データ投入

```bash
# ローカルで実行、または Vercel Functions から
npx ts-node scripts/seed-data.ts
```

### 7.5 Claude.aiへの登録

1. [Claude.ai](https://claude.ai) にログイン
2. 左下の「⚙️」→「Integrations」
3. 「Add custom connector」をクリック
4. 入力:
   - **名前**: `Nano Banana Prompts`
   - **URL**: `https://your-project.vercel.app/api/mcp`
5. 「追加」をクリック

---

## 8. 運用・メンテナンス

### 8.1 データ更新

| 項目 | 頻度 | 方法 |
|------|------|------|
| 自動同期 | 日次（0:00 UTC） | Vercel Cron Jobs |
| 手動同期 | 必要時 | `/api/sync` にPOSTリクエスト |

### 8.2 モニタリング

- **Vercel Dashboard**: デプロイ状況、エラーログ
- **Upstash Console**: ベクトル数、クエリ数

### 8.3 トラブルシューティング

| 症状 | 原因 | 対処 |
|------|------|------|
| 検索結果が返らない | ベクトルDBが空 | `/api/sync` を手動実行 |
| タイムアウト | 10秒制限超過 | クエリ最適化、limit削減 |
| Claude.aiで接続できない | URL間違い、認証 | URLを再確認 |

---

## 9. 共有・配布方法

### 9.1 URL共有（推奨）

```
MCPサーバーURL: https://your-project.vercel.app/api/mcp
```

他のユーザーはこのURLをClaude.aiのIntegrationsに追加するだけで利用可能。

### 9.2 セルフホスト

GitHubリポジトリをForkして各自のVercelアカウントでデプロイ。

**メリット**:
- 各自の無料枠を使用
- カスタマイズ可能

**デメリット**:
- 各自でUpstashアカウント作成が必要
- 技術的知識が必要

---

## 10. 将来の拡張案

| 機能 | 優先度 | 説明 |
|------|--------|------|
| プロンプト追加API | 中 | ユーザーが独自プロンプトを追加 |
| お気に入り機能 | 低 | よく使うプロンプトを保存 |
| 多言語対応強化 | 中 | 日本語プロンプトの充実 |
| 画像プレビュー | 低 | 検索結果にサンプル画像表示 |

---

## 11. 参考リンク

- [MCP公式ドキュメント](https://modelcontextprotocol.io/)
- [Vercel Remote MCP Server Guide](https://vercel.com/docs)
- [Upstash Vector Documentation](https://upstash.com/docs/vector)
- [ZeroLu/awesome-nanobanana-pro](https://github.com/ZeroLu/awesome-nanobanana-pro)
- [xianyu110/awesome-nanobananapro-prompts](https://github.com/xianyu110/awesome-nanobananapro-prompts)

---

## 付録: コスト試算

### 無料枠での運用

| サービス | 使用量/月 | 無料枠 | 超過時 |
|---------|----------|--------|--------|
| Vercel | 〜1GB帯域 | 100GB | $40/100GB |
| Upstash Vector | 〜1,000ベクトル | 10,000 | $0.4/10万 |
| Upstash クエリ | 〜3,000回 | 10,000回/日 | $0.4/10万 |

**結論**: 個人〜小規模利用なら完全無料で運用可能。
